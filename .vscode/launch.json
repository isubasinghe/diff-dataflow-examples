{

    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Neighbourhood",
            "program": "${workspaceRoot}/target/debug/neighbourhood.exe",
            "args": ["./livejournal.txt"],
            "cwd": "${workspaceRoot}/target/debug/",
            "sourceLanguages": ["rust"]
        }
    ]
}